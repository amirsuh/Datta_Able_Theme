import { DateageingPipe } from './dateageing.pipe';

describe('DateageingPipe', () => {
  it('create an instance', () => {
    const pipe = new DateageingPipe();
    expect(pipe).toBeTruthy();
  });
});
