import { DateageingrefundPipe } from './dateageingrefund.pipe';

describe('DateageingrefundPipe', () => {
  it('create an instance', () => {
    const pipe = new DateageingrefundPipe();
    expect(pipe).toBeTruthy();
  });
});
